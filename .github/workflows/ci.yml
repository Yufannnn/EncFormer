name: EncFormer

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  static-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Lint
        run: |
          pip install --quiet ruff
          ruff check src/ scripts/ tests/

      - name: Syntax
        run: |
          python - <<'PY'
          import ast, glob, sys
          bad = 0
          for f in glob.glob("scripts/*.py"):
              try:
                  ast.parse(open(f).read())
              except SyntaxError as e:
                  print(f"SYNTAX ERROR {f}: {e}"); bad += 1
          sys.exit(1 if bad else 0)
          PY

      - name: Shell
        run: bash -n scripts/build.sh scripts/2pc.sh scripts/build_ezpc_sci.sh scripts/package.sh

      - name: Parameters
        run: |
          test -f third_party/phantom-fhe/src/prng.cu || { echo "phantom source missing"; exit 1; }
          grep -q "sample_ternary_poly" third_party/phantom-fhe/src/prng.cu || { echo "sparse-ternary sampler missing"; exit 1; }
          grep -q "hamming_weight"      third_party/phantom-fhe/src/prng.cu || { echo "hamming-weight bound missing"; exit 1; }
          grep -q "ring_bits: int = 43" src/engines/mpc_engine_ezpc.py || { echo "ell=43 changed"; exit 1; }
          grep -q "scale_bits: int = 13" src/engines/mpc_engine_ezpc.py || { echo "F=13 changed"; exit 1; }
          echo "params + security markers present"

      - name: Package
        run: |
          for f in scripts/demo.py scripts/eval.py scripts/check.py \
                   Dockerfile requirements.txt CHECKSUMS.sha256 \
                   assets/EncFormer-Profile.png data/sst2-validation.jsonl \
                   data/bert-base-uncased/vocab.txt checkpoints/encformer-sst2/model.pt \
                   third_party/ezpc-sci/src/binding.cpp \
                   third_party/phantom-fhe/examples/pipe_ckks_attn.cu \
                   third_party/ezpc/SCI/src/FloatingPoint/fixed-point.cpp ; do
            test -f "$f" || { echo "MISSING tracked file: $f"; exit 1; }
          done
          bash scripts/package.sh dist
          unzip -tq dist/EncFormer-v1.0.0.zip
